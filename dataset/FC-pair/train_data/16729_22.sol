contract c16729{
  /**
   * @dev check crowdsale status
   */
  function checkStatus() public view returns(uint256 weiRaised_, uint256 rate_, address wallet_, uint256 periodSales_) {
    return (weiRaised,rate,wallet,periodSales);
  }
}